"use strict"; function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }


var _chunkTW4UE7BUjs = require('../chunk-TW4UE7BU.js');

// src/node/cli.ts
var _yargs = require('yargs'); var _yargs2 = _interopRequireDefault(_yargs);

// src/node/build.ts
var _path = require('path');
var _chalk = require('chalk'); var _chalk2 = _interopRequireDefault(_chalk);
var _fsextra = require('fs-extra'); var _fsextra2 = _interopRequireDefault(_fsextra);
var _vite = require('vite');
var _serverrenderer = require('@vue/server-renderer');
var _jsdom = require('jsdom');

// src/node/preload-links.ts
function renderPreloadLinks(document, modules, ssrManifest) {
  const seen = new Set();
  const preloadLinks = [];
  Array.from(modules).forEach((id) => {
    const files = ssrManifest[id] || [];
    files.forEach((file) => {
      if (!preloadLinks.includes(file))
        preloadLinks.push(file);
    });
  });
  if (preloadLinks) {
    preloadLinks.forEach((file) => {
      if (!seen.has(file)) {
        seen.add(file);
        renderPreloadLink(document, file);
      }
    });
  }
}
function renderPreloadLink(document, file) {
  if (file.endsWith(".js")) {
    appendLink(document, {
      rel: "modulepreload",
      crossOrigin: "",
      href: file
    });
  } else if (file.endsWith(".css")) {
    appendLink(document, {
      rel: "stylesheet",
      href: file
    });
  }
}
var createLink = (document) => document.createElement("link");
var setAttrs = (el, attrs) => {
  const keys = Object.keys(attrs);
  for (const key of keys)
    el.setAttribute(key, attrs[key]);
};
function appendLink(document, attrs) {
  const exits = document.head.querySelector(`link[href='${attrs.file}']`);
  if (exits)
    return;
  const link = createLink(document);
  setAttrs(link, attrs);
  document.head.appendChild(link);
}

// src/node/utils.ts

function buildLog(text, count) {
  console.log(`
${_chalk2.default.gray("[vite-ssg]")} ${_chalk2.default.yellow(text)}${count ? _chalk2.default.blue(` (${count})`) : ""}`);
}
function getSize(str) {
  return `${(str.length / 1024).toFixed(2)} KiB`;
}
function routesToPaths(routes) {
  if (!routes)
    return ["/"];
  const paths = new Set();
  const getPaths = (routes2, prefix = "") => {
    prefix = prefix.replace(/\/$/g, "");
    for (const route of routes2) {
      let path = route.path;
      if (route.path) {
        path = prefix && !route.path.startsWith("/") ? `${prefix}/${route.path}` : route.path;
        paths.add(path);
      }
      if (Array.isArray(route.children))
        getPaths(route.children, path);
    }
  };
  getPaths(routes);
  return [...paths];
}

// src/node/critical.ts
function getCritters(outDir, options = {}) {
  try {
    const CrittersClass = _chunkTW4UE7BUjs.__require.call(void 0, "critters");
    return new CrittersClass(_chunkTW4UE7BUjs.__spreadValues.call(void 0, {
      path: outDir,
      logLevel: "warn",
      external: true,
      inlineFonts: true,
      preloadFonts: true
    }, options));
  } catch (e) {
    return void 0;
  }
}

// src/node/build.ts
function DefaultIncludedRoutes(paths) {
  return paths.filter((i) => !i.includes(":") && !i.includes("*"));
}
function readJson(path) {
  return JSON.parse(_fsextra2.default.readFileSync(path, "utf8"));
}
async function build(cliOptions = {}) {
  var _a;
  const mode = process.env.MODE || process.env.NODE_ENV || cliOptions.mode || "production";
  const config = await _vite.resolveConfig.call(void 0, {}, "build", mode);
  const cwd = process.cwd();
  const root = config.root || cwd;
  const ssgOut = _path.join.call(void 0, root, ".vite-ssg-temp");
  const outDir = config.build.outDir || "dist";
  const out = _path.isAbsolute.call(void 0, outDir) ? outDir : _path.join.call(void 0, root, outDir);
  const isTypeModule = readJson(_path.resolve.call(void 0, cwd, "package.json")).type === "module";
  const {
    script = "sync",
    mock = false,
    entry = await detectEntry(root),
    formatting = "none",
    crittersOptions = {},
    includedRoutes = DefaultIncludedRoutes,
    onBeforePageRender,
    onPageRendered,
    onFinished,
    dirStyle = "flat",
    includeAllRoutes = false
  } = Object.assign({}, config.ssgOptions || {}, cliOptions);
  if (_fsextra2.default.existsSync(ssgOut))
    await _fsextra2.default.remove(ssgOut);
  buildLog("Build for client...");
  await _vite.build.call(void 0, {
    build: {
      ssrManifest: true,
      rollupOptions: {
        input: {
          app: _path.join.call(void 0, root, "./index.html")
        }
      }
    },
    mode: config.mode
  });
  buildLog("Build for server...");
  process.env.VITE_SSG = "true";
  const ssrEntry = await resolveAlias(config, entry);
  await _vite.build.call(void 0, {
    build: {
      ssr: ssrEntry,
      outDir: ssgOut,
      minify: false,
      cssCodeSplit: false,
      rollupOptions: {
        output: {
          entryFileNames: `[name].${isTypeModule ? "cjs" : "js"}`
        }
      }
    },
    mode: config.mode
  });
  const { createApp } = _chunkTW4UE7BUjs.__require.call(void 0, _path.join.call(void 0, ssgOut, `${_path.parse.call(void 0, ssrEntry).name}.${isTypeModule ? "cjs" : "js"}`));
  const { routes } = await createApp(false);
  let routesPaths = includeAllRoutes ? routesToPaths(routes) : await includedRoutes(routesToPaths(routes));
  routesPaths = Array.from(new Set(routesPaths));
  buildLog("Rendering Pages...", routesPaths.length);
  const critters = crittersOptions !== false ? getCritters(outDir, crittersOptions) : void 0;
  if (critters)
    console.log(`${_chalk2.default.gray("[vite-ssg]")} ${_chalk2.default.blue("Critical CSS generation enabled via `critters`")}`);
  if (mock) {
    const virtualConsole = new (0, _jsdom.VirtualConsole)();
    const jsdom = new (0, _jsdom.JSDOM)("", { url: "http://localhost", virtualConsole });
    global.window = jsdom.window;
    Object.assign(global, jsdom.window);
  }
  const ssrManifest = JSON.parse(await _fsextra2.default.readFile(_path.join.call(void 0, out, "ssr-manifest.json"), "utf-8"));
  let indexHTML = await _fsextra2.default.readFile(_path.join.call(void 0, out, "index.html"), "utf-8");
  indexHTML = rewriteScripts(indexHTML, script);
  await Promise.all(routesPaths.map(async (route) => {
    try {
      const { app, router, head, initialState } = await createApp(false, route);
      if (router) {
        await router.push(route);
        await router.isReady();
      }
      const transformedIndexHTML = await (onBeforePageRender == null ? void 0 : onBeforePageRender(route, indexHTML)) || indexHTML;
      const ctx = {};
      const appHTML = await _serverrenderer.renderToString.call(void 0, app, ctx);
      const renderedHTML = renderHTML({ indexHTML: transformedIndexHTML, appHTML, initialState });
      const jsdom = new (0, _jsdom.JSDOM)(renderedHTML);
      renderPreloadLinks(jsdom.window.document, ctx.modules || new Set(), ssrManifest);
      head == null ? void 0 : head.updateDOM(jsdom.window.document);
      const html = jsdom.serialize();
      let transformed = await (onPageRendered == null ? void 0 : onPageRendered(route, html)) || html;
      if (critters)
        transformed = await critters.process(transformed);
      const formatted = format(transformed, formatting);
      const relativeRouteFile = `${(route.endsWith("/") ? `${route}index` : route).replace(/^\//g, "")}.html`;
      const filename = dirStyle === "nested" ? _path.join.call(void 0, route.replace(/^\//g, ""), "index.html") : relativeRouteFile;
      await _fsextra2.default.ensureDir(_path.join.call(void 0, out, _path.dirname.call(void 0, filename)));
      await _fsextra2.default.writeFile(_path.join.call(void 0, out, filename), formatted, "utf-8");
      config.logger.info(`${_chalk2.default.dim(`${outDir}/`)}${_chalk2.default.cyan(filename.padEnd(15, " "))}  ${_chalk2.default.dim(getSize(formatted))}`);
    } catch (err) {
      throw new Error(`${_chalk2.default.gray("[vite-ssg]")} ${_chalk2.default.red(`Error on page: ${_chalk2.default.cyan(route)}`)}
${err.stack}`);
    }
  }));
  await _fsextra2.default.remove(ssgOut);
  const pwaPlugin = (_a = config.plugins.find((i) => i.name === "vite-plugin-pwa")) == null ? void 0 : _a.api;
  if (pwaPlugin == null ? void 0 : pwaPlugin.generateSW) {
    buildLog("Regenerate PWA...");
    await pwaPlugin.generateSW();
  }
  console.log(`
${_chalk2.default.gray("[vite-ssg]")} ${_chalk2.default.green("Build finished.")}`);
  await (onFinished == null ? void 0 : onFinished());
  const waitInSeconds = 15;
  const timeout = setTimeout(() => {
    console.log(`${_chalk2.default.gray("[vite-ssg]")} ${_chalk2.default.yellow(`Build process still running after ${waitInSeconds}s. There might be something misconfigured in your setup. Force exit.`)}`);
    process.exit(0);
  }, waitInSeconds * 1e3);
  timeout.unref();
}
function rewriteScripts(indexHTML, mode) {
  if (!mode || mode === "sync")
    return indexHTML;
  return indexHTML.replace(/<script type="module" /g, `<script type="module" ${mode} `);
}
function renderHTML({ indexHTML, appHTML, initialState }) {
  const stateScript = initialState ? `
<script>window.__INITIAL_STATE__=${initialState}<\/script>` : "";
  return indexHTML.replace('<div id="app"></div>', `<div id="app" data-server-rendered="true">${appHTML}</div>${stateScript}`);
}
function format(html, formatting) {
  if (formatting === "minify") {
    return _chunkTW4UE7BUjs.__require.call(void 0, "html-minifier").minify(html, {
      collapseWhitespace: true,
      caseSensitive: true,
      collapseInlineTagWhitespace: false,
      minifyJS: true,
      minifyCSS: true
    });
  } else if (formatting === "prettify") {
    return _chunkTW4UE7BUjs.__require.call(void 0, "prettier").format(html, { semi: false, parser: "html" });
  }
  return html;
}
async function detectEntry(root) {
  const scriptSrcReg = /<script(?:.*?)src=["'](.+?)["'](?!<)(?:.*)\>(?:[\n\r\s]*?)(?:<\/script>)/img;
  const html = await _fsextra2.default.readFile(_path.join.call(void 0, root, "index.html"), "utf-8");
  const scripts = [...html.matchAll(scriptSrcReg)];
  const [, entry] = scripts.find((matchResult) => {
    const [script] = matchResult;
    const [, scriptType] = script.match(/.*\stype=(?:'|")?([^>'"\s]+)/i) || [];
    return scriptType === "module";
  }) || [];
  return entry || "src/main.ts";
}
async function resolveAlias(config, entry) {
  const resolver = config.createResolver();
  const result = await resolver(entry, config.root);
  return result || _path.join.call(void 0, config.root, entry);
}

// src/node/cli.ts
_yargs2.default.scriptName("vite-ssg").usage("$0 [args]").command("build", "Build SSG", (args) => args.option("script", {
  choices: ["sync", "async", "defer", "async defer"],
  describe: "Rewrites script loading timing"
}).option("mock", {
  type: "boolean",
  describe: "Mock browser globals (window, document, etc.) for SSG"
}), async (args) => {
  await build(args);
}).showHelpOnFail(false).help().argv;
